[tasks."postgres:start"]
description = "Start local Postgres for development"
run = "docker run -d --name timescaledb -p 6543:5432 -e POSTGRES_PASSWORD=password timescale/timescaledb-ha:pg18"

[tasks."postgres:stop"]
description = "Stop local Postgres"
run = "docker rm -f timescaledb"
